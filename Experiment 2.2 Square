#include <stdio.h>
 int main()
 {
     int s,a,p;
     printf("Enter side of square:");
     scanf("%d",&s);
     a=s*s;     
     p=4*s;
     printf("Area = %d  perimeter = %d",a,p);
 return 0;
 }
